# Demo Asset Guide

This folder is reserved for optional local-only demo recordings, such as `auth-flow.gif`.

Only add a demo after reviewing that it contains:

- fake local credentials only
- no tokens or secret values
- no production URLs
- no Apple credentials
- no private local paths
- no real user data

Do not upload recordings to external services as part of repository verification.
